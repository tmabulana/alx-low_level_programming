#include <stdio.h>

/**
 * main - the main function to be used is this one
 *
 * Return: 0 always
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
