#ifndef MACROLIKE_H
#define MACROLIKE_H
/**
 * File: 3-function_like_macro
 * @x: the input value (an int)
 * Desc: the header file that computes the exact value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))
#endif
