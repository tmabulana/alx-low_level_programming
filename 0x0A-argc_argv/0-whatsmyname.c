#include <stdio.h>
/**
 * main - has to print its name, followed by a new line
 * @argc: the number of commands on line argument
 * @argv: the array that contains the command line argument
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{

printf("%s\n", argv[0]);
return (0);
}
